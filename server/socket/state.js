/**
 * @type {SocketSharedState['boardIdMap']}
 */
const boardIdMap = new Map();

/**
 * @type {SocketSharedState['usernameMap']}
 */
const usernameMap = {};

module.exports = {
    boardIdMap,
    usernameMap,
};
